#lang racket
(provide (all-defined-out))
;; TDA element ;;
;Representacion:
; elemento

;; Capa Constructora ;;
;Descripcion: Constructor del elemento
;Dom: string
;Rec: string
(define element
  (lambda (x)
    x))